# ── Secrets — never commit real credentials ────────────────────────────────────
secrets.yaml

# ── ESPHome build artifacts ───────────────────────────────────────────────────
.esphome/
__pycache__/
*.pyc

# ── Proto fetch cache (re-generated by scripts/gen_proto.sh) ─────────────────
# Remove the lines below if you prefer to commit generated proto files.
# Committing them avoids requiring protoc on every machine.
# .proto_src/

# ── Editor / OS ───────────────────────────────────────────────────────────────
.DS_Store
.vscode/
*.swp
*.swo
